<Window x:Class="PersonalLocalAIWorkstation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Personal Local AI Workstation v2.0 (ADVANCED SUITE) — Hsini Mohamed"
        Height="850" Width="1350"
        WindowStartupLocation="CenterScreen"
        Background="#060911">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Native Dark Command Bar -->
        <Border Grid.Row="0" Background="#0A0E1A" BorderBrush="#1E293B" BorderThickness="0,0,0,1" Padding="14,8">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="pack://application:,,,/assets/app_icon.png" Width="22" Height="22" Margin="0,0,10,0"/>
                    <TextBlock Text="Personal Local AI Workstation" Foreground="#F8FAFC" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                    <Border Background="#1A00E5FF" BorderBrush="#3300E5FF" BorderThickness="1" CornerRadius="6" Padding="6,2" Margin="8,0,0,0">
                        <TextBlock Text="v2.0 PRO" Foreground="#00E5FF" FontFamily="Consolas" FontSize="10" FontWeight="Bold"/>
                    </Border>
                    <Border Background="#1A10B981" BorderBrush="#3310B981" BorderThickness="1" CornerRadius="6" Padding="6,2" Margin="6,0,0,0">
                        <TextBlock Text="OFFLINE ACTIVE" Foreground="#34D399" FontFamily="Consolas" FontSize="10" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <!-- Quick Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Click="BtnOpenBrowser_Click" Margin="0,0,8,0" Background="#1E293B" BorderBrush="#334155" Foreground="#CBD5E1" FontFamily="Consolas" FontSize="11" Padding="12,5" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🌐 "/>
                            <TextBlock Text="Open in Browser"/>
                        </StackPanel>
                    </Button>

                    <Button Click="BtnAskDeveloper_Click" Margin="0,0,8,0" Background="#2E1065" BorderBrush="#7C3AED" Foreground="#C084FC" FontFamily="Consolas" FontSize="11" Padding="12,5" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👨‍💻 "/>
                            <TextBlock Text="Ask Developer"/>
                        </StackPanel>
                    </Button>

                    <Button Click="BtnRefresh_Click" Background="#1E293B" BorderBrush="#334155" Foreground="#94A3B8" FontFamily="Consolas" FontSize="11" Padding="8,5" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="🔄"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Embedded Ultra-Fast Microsoft WebView2 Host -->
        <wv2:WebView2 x:Name="WebViewControl" Grid.Row="1" Source="http://localhost:3009"/>
    </Grid>
</Window>
